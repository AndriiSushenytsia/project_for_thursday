<p id="text"><%= post.content %> </p>
<p id="small-text">Created at: <%= post.created_at %> </p>
<p id="small-text">Author: <%= post.user.first_name %> <%= post.user.last_name %></p>
<% if user_signed_in? && current_user.id == post.user_id%>
  <%= link_to "Edit", edit_current_post_path(post.id) %> | 
  <%= link_to "Delete", current_post_path(post.id), method: :delete, data: { confirm: "Are you sure?" } %>
<%end%>
<p>_______________</p>
